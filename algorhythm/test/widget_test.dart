import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (tester) async {
    // Unit tests for SRS engine, streak, and mastery logic are in test/unit/
    expect(true, isTrue);
  });
}
